<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="main w-full h-full p-4 flex flex-col items-center justify-center gap-6">
        <div class="profile w-screen h-[25vh] flex items-center justify-center gap-4">
            <div class="flex flex-col gap-4">
                <div class="w-32 h-32 rounded-full bg-[#F2F4F7]">
                    <%if(user.profile_img && user.profile_img != "undefined"){%>
                        <img class="object-contain h-full w-full rounded-full" src="<%=user.profile_img%>" alt="User Profile">
                    <%}else{%>
                        <img class="object-contain h-full w-full rounded-full" src="https://static.vecteezy.com/system/resources/thumbnails/009/292/244/small_2x/default-avatar-icon-of-social-media-user-vector.jpg">
                    <%}%>
                </div>
                <div class="info flex items-center justify-center flex-col">
                    <p class="text-[#606366] tracking-tighter">@<%=user.username%></p>
                    <p class="text-lg font-semibold tracking-tighter"><%=user.name%></p>
                </div>
            </div>
            <div class="flex gap-4">
                <a class="w-32 h-8 bg-red-600 text-white flex items-center justify-center rounded-md" href="/logout">Logout</a>
            </div>
        </div>
        <form class="w-[60%] h-[60vh] bg-[#F2F4F7] rounded-xl shadow-sm shadow-zinc-900 flex items-center justify-center flex-col gap-8" action="/upload" method="post">
            <h3 class="text-lg font-semibold tracking-tighter pb-[-40px]">Upload Profile Picture</h3>
            <textarea class="w-[90%] h-[40%] border-2 border-zinc-300 inset-shadow-sm inset-shadow-zinc-400 outline-none text-xl rounded-xl p-4 tracking-tighter pb-[-40px] text-start resize-none" placeholder="Enter image url" name="profile_img"></textarea>
            <input class="w-30 h-8 bg-yellow-600 text-white rounded-md" type="submit" value="Upload">
        </form>
    </div>
</body>
</html>